import React from "react";
import { Layout } from "antd";
import { PageLayout } from "../../components/PageLayout";

const { Header } = Layout;

export const UserPage: React.FC = () => {
  return (
    <PageLayout>
      <p>User page working on</p>
    </PageLayout>
  );
};

// export const LeaderboardPage = () => {
//   return <div>Leaderboard</div>;
// };
